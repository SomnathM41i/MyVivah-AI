# MyVivahAI — Changelog

> Format: Keep a Changelog style (https://keepachangelog.com/en/1.1.0/).

### Added — 2026-09-25 (External platform user search and chat integration)

- Added the versioned external search contract (`docs/external-user-search-v1.md`) and Core PHP/PDO sample. Client APIs authenticate the platform and requester server-to-server, apply visibility and chat eligibility, return minimal fields, and paginate with opaque cursors.
- Added endpoint, host allowlist, and encrypted credential fields to the existing `platform_integrations` row. Added a backend HTTP client with bounded timeouts, streamed response-size limit, schema validation, rate limiting, redirects disabled, endpoint restrictions, and redacted outcome logging.
- Added `GET /api/v1/widget/users/search`, widget search results with thumbnail/display name/Load more/loading/empty/error states, and a short-lived encrypted candidate token. Conversation creation rejects raw target IDs from widget requests, rechecks live eligibility with the client API, creates a previously unmapped recipient reference, then reuses deterministic conversation creation.
- Added dashboard search endpoint/credential form, a safe sample-query connection test, and `/api/v1/health/chat` database readiness check. MySQL remains authoritative; REST polling remains the shared-hosting fallback; realtime defaults off.
- **Verified:** 176 PHPUnit tests / 1,351 assertions on SQLite; Pint `--test` passed; PHPStan level 5 passed; touched PHP files and Core PHP samples passed `php -l`.
- **Not verified:** MySQL test suite not run because `phpunit.mysql.xml` targets `myvivah` and tests use `RefreshDatabase`; Node widget tests/build unavailable because `node`/`npm` are not installed; Hostinger deployment, backups/restores, cron, and live WebSocket delivery were not inspected.

### Added — 2026-09-24 (Phase 5A — Public Website, Web Auth & App Shell)

- Completed the client-facing frontend layer per `docs/phase-5a-public-website-auth.md`. **Stack deviation (documented):** The 2026 outline mentions Laravel/Inertia/React/TS, but this project has always shipped Blade + Tailwind v4 + Vite + vanilla JS, and `docs/AGENTS.md` forbids unnecessary dependencies — so Phase 5A is **server-rendered Blade**, not an Inertia/SPA app. Fully tested + exit-verified on sqlite and MySQL.
  - **Public marketing site:** Home, About, Services, Plans, Contact — all rendered through `x-layout.guest` (which emits `<title>`/`meta.description`/canonical/OG/Twitter from each controller's `meta` array) with `marketing-nav` + `marketing-footer` partials, `public/favicon.svg`; **no invented content** — live services render from `services` (single source of truth — `is_active` drives both the page and API entitlements), plans are DB-driven (ADR-012), and the Services roadmap group is controller copy explicitly badged *Coming soon*.
  - **Design system (Blade components):** `card`, `badge`, `alert`, `field` (renders its own input; password values never repopulate), `section-heading`, `flash` (session flash + validation errors + verified/sent banners), `logo`, `icon` (fixed a stray `]` that broke `icon-eye-off`), `button` (+ `light`/`light-outline`); layouts `components/layout/{guest,auth,dashboard}`.
  - **Web auth (server-side sessions, all Blade):** signup creates user + platform + `platform_admins` owner in one transaction (`PlatformAccountService`) then dispatches `VerifyEmailNotification`; `RegisteredUserController` follows the verified-user rule, hashed password cast. Login/logout. Forgot/reset password via `ResetPasswordNotification` + `Password::Reset`. Email verification via signed URL (`hash = sha1(email)`) + `verification.notice` confirmation + `verification.verify` resend (throttled 3:1).
  - **Security/correctness:** `App\Models\User` now `implements MustVerifyEmail` — without it the `verified` middleware silently passed for unverified users (framework base `User` trait lacks the contract). `verification.notice` GET is **outside** the guest group so `verified`-middleware redirects work. Forgot-password + verify-resend return neutral status text (no account enumeration). POST flows throttled (`login`/`signup` 6:1, password routes 5:1, contact 6:1). Contact form: honeypot `website` field (`max:0`) + only `ip_hash = sha256(ip)` stored (no raw IPs); `ContactMessage` is append-only (no updated_at). Guest redirect targets route **`dashboard`** (renamed from `dashboard.index`).
  - **Dashboard app shell (Blade, honest empty-states):** overview (platform + subscription summary via `platform-summary` partial), services (real `platform_service_access` entitlements from DB), integrations (`PlatformIntegration::primaryApiKey()->secret_fingerprint` + widget embed instructions + PASETO scopes), subscription (DB rows; statuses `pending/active/expired/cancelled/suspended`, **no `trialing`** — controller filters `status='active'` + `ends_at` null/future), payments (`GatewayTransactionId`/`paid_at`/`amount`), settings (profile summary; editing is follow-up). Per ADR-011 the dashboard never fakes activation — checkout arrives in a later phase.
  - **Schema (20th migration on MySQL):** `2026_09_24_000001_create_contact_messages_table` (`name`/`email`/`subject`/`message` TEXT/`ip_hash` CHAR(64)+UNIQUE/`created_at` only); `ContactMessage` model in `app/Models`.
  - **Tests (6 new files, ~177 assertions):** `PublicPagesTest` (all pages 200 + expected headings + plans render DB plans — uses `ServiceFactory::active()`/`PlanFactory::active()`, no seeded data), `ContactSubmissionTest` (stores + mail sent via `Mail::fake()`, honeypot rejected, empty subject/overlong rejected), `AuthUserRegistrationTest` (signup → platform + owner admin + unverified + `assertNotAuthenticated` + `VerifyEmail` queued; invalid/duplicate 422), `AuthLoginTest` (unverified block + verified login + `intended()` redirect + suspended block), `PasswordResetTest` (forgot neutral + reset token via `Password::Reset` + new password works), `DashboardAccessTest` (guest → redirect login, unverified → `verification.notice`, verified → sections render, `platform-summary` shows DB numbers).
  - **Verification (all green):** `php -l` clean on every changed file · Pint (Laravel preset) `--test` **PASS (187 files)** · PHPStan level 5 **No errors** · full PHPUnit suite **166 tests / 1285 assertions** on sqlite AND real MySQL `myvivah` · `migrate:fresh --force` **20**/20 on MySQL · `npm run test:widget` **15/15** · `vite build` success (asset manifest regenerated) · `route:list` confirms the full public/auth/dashboard set.
  - **Fixes found by tests and fixed:** `MustVerifyEmail` contract (§above); `config/mail.php` needs a `to` array with `name` key or `MailManager` crashes (`Undefined array key "name"`); `phpunit.xml` now sets `MAIL_TO_ADDRESS`; plans page must render raw `&#039;`-safe text via `assertSee('Register a platform under Settings')` to avoid apostrophe escaping; `field` component double-value attribute + password repopulation eliminated; `icon-eye-off` stray `]`.
- **Stop boundary honored:** no payment gateway/checkout code path, no admin dashboard, no profile editing, no API-testing screen UI, no search delegation, no AI agents — the app shell shows read-only honest states for what is not yet implemented.

### Added — 2026-09-23 (Phase 4 — Embeddable Chat Widget: session bootstrap + client JS)

- Completed the Phase 4 widget layer per `docs/widget-integration.md` (§Phase 4) — **short-lived single-user widget sessions, REST-first with optional realtime, zero-dependency client JS, local demo**:
  - **Session bootstrap:** `POST /api/v1/widget/session` (platform PASETO, must hold `realtime_chat:write`) mints a widget session token = **PASETO v4.local**, `aud = widget:{slug}`, `sub = external_user_id`, claims `platform_id`/`external_user_id`/`scope=[realtime_chat:read,realtime_chat:write]`, jti, footer `kid`; TTL `config('widget.session.ttl_seconds')` default 900 capped at `max_ttl_seconds` (1800). The port 900s browser token is the ONLY thing a platform backend ever sends to the browser — its PASETO secret/API keys never leave the client server.
  - **Services/config:** `WidgetSessionService` (issue/validate/revoke/present; `keyFromToken` + v4-only `parseVersion4Local` seams added to `PasetoTokenService`), `ValidatedWidgetSession`, `IssuedWidgetSession`, `WidgetRealtimeConfig` (realtime coordinates from `config/chat.php` with `broadcasting.default` fallback), `TokenKeyContext`, `config/widget.php` + `config/cors.php` (`api/v1/widget/*` preflights, wildcard allowed, no credentials).
  - **Middleware/identity:** `ValidateWidgetToken` sets BOTH `widget_session` + an adapted `paseto` so `EnsurePlatformAccess`/`LogApiAudit`/`EnforcePlatformRateLimit` run unchanged; `RestrictWidgetOrigins` **prepended to the GLOBAL stack** (runs after `HandleCors` on the way out) strips ACAO for origins not in `integration.allowed_origins` (exact or `https://*.sub.example` — discovered mid-test that the wildcard is a host wildcard, not a leading `*.`); `ExternalUserContext` widget fast-path → acting user ALWAYS the token-bound user, spoofed `X-External-User-Id` ignored; widget `conversations.store` guard: actor must be a participant (422).
  - **Widget API routes (`api/v1/widget/**`):** conversations CRUD/read, messages index/store (idempotent by `client_message_id`), read/markRead, presence heartbeat/show/me, `socket/auth`, `integration/users` (+ `?q=` substring filter, LIKE-escaped with explicit `ESCAPE` clause for SQLite parity), `users/{external_user_id}`, `session/revoke` (jti blacklist). `presence/me` registered before the `{external_user_id}` wildcard (route-order fix).
  - **Client JS:** `public/js/myvivah-widget.js` — zero-dependency IIFE, `window.MyVivahAIWidget.init/open/close/toggle/destroy/get/version`; pure `_core` helpers unit-tested in `node:vm` (no DOM). REST-first with optional realtime (minimal pusher-protocol WS client, signing via `socket/auth`, backoff, halt→polling after `realtimeMaxReconnectAttempts`); optimistic sends with `sending/sent/failed` + tap-to-retry; `client_message_id` dedupe on ack; presence dots via REST polling (platform public_id is not transported); session expiry → optional `sessionEndpoint` refresh.
  - **Demo:** `GET /demo/chat` (local + APP_DEBUG only, else 404) renders Alice + Bob side-by-side; `DemoPlatformSeeder` idempotently seeds demo integration + primary key + 5 demo users; `ChatDemoController` + `demo/chat` + `demo/chat-error` views.
  - **Bugs found by tests and fixed:** `WidgetSessionService::issue` type-hint referenced the wrong `PlatformApiKey` namespace (missing import → TypeError in every widget test); SQLite LIKE has no default escape char → added explicit `ESCAPE '\'`; `RestrictWidgetOrigins` had to move OUT of the route stack (HandleCors adds ACAO headers after route middleware on the response path) — now a global prepend; demo guard reads `config('app.env')` so tests can flip local mode; Pagination/search `q=%` semantics; message history `sender` is an object (`sender.external_user_id`), broadcasts use `sender_id`.
  - **Environment note (tooling):** a stray shell redirect truncated `C:\xampp\php\php.exe` during this session; restored from the matching official `php-8.2.12-Win32-vs16-x64` build.
- **Tests (33 new; ~316 assertions):** `tests/Feature/WidgetApiTest.php` (bootstrap, short lifecycle, write-scope gate, cross-route isolation both ways, spoofed-header identity, platform-scoped + LIKE-escaped search, revoke-then-reject, expired reject, cross-platform isolation) + `tests/Feature/WidgetIntegrationTest.php` (E2E open/send/read idempotency, participant guard, unread/markRead, presence + socket-auth token identity incl. spoofed-header denial, realtime broadcast parity, CORS preflight only on widget paths, origin restriction strip/keep, demo local-only 404/200) + `tests/js/widget-core.test.js` (15: config, urls, clamp, createClientMessageId, esc, avatar/display, mergeMessages dedupe+sort, parseWsMessage, eventToMessage, formatRelative, reconnect delay, auth-error) — assertions compared cross-realm via JSON (`deepStrictEqual` fails across `node:vm` realms).
- **Verification (all green):** `php -l` clean on every changed file · Pint (Laravel preset) `--test` **PASS** (157 files) · PHPStan level 5 **No errors** · full PHPUnit suite **126 tests / 1107 assertions** on sqlite AND real MySQL `myvivah` · **15/15** Node widget tests · `php artisan route:list --path=widget` shows the 14 widget routes.
- **Docs:** `docs/widget-integration.md` (§Phase 4 implementation), `docs/integration-api.md` (widget endpoints), `docs/security.md` (widget trust model + CORS), `docs/deployment.md` (widget static asset + realtime coordinate env), `.env.example` (`CHAT_REALTIME_CONNECTION/SCHEME/HOST/PORT/PUSHER_CLUSTER`, `WIDGET_SESSION_TTL_SECONDS`), `docs/changelog.md` (this), `docs/current-tasks.md`, `docs/known-issues.md`, `docs/README.md`.
- **Stop boundary honored:** subscriptions/payments, admin dashboard, widget config UI, external search-API delegation, AI agents were **NOT** started (only the browser-facing API + demo UI shipped).

### Added — 2026-09-23 (Phase 3E — Realtime Messaging: broadcast + presence + channel auth)

- Completed the optional realtime layer per `docs/realtime-chat.md` (§Real-Time Transport / §Presence) in **one batch** — **Redis-optional, shared-hosting friendly, REST-always**:
  - **Broadcast config:** `config/broadcasting.php` (default **`null`** no-op connection; pre-wired `reverb`/`soketi`/`pusher`/`ably`/`redis`/`log` lanes — pusher-protocol defaults from `REVERB_*`; enabling any pusher lane only needs `composer require pusher/pusher-php-server`, + `laravel/reverb` for the server binary) + `config/chat.php` `realtime` block (`enabled`, `app_key`/`app_secret`, channel names, presence tunables). Env: `.env`/`.env.example` `BROADCAST_CONNECTION=null`, `CHAT_REALTIME_ENABLED=false`, `CHAT_PRESENCE_OFFLINE_AFTER=90`.
  - **Schema:** migration `2026_09_23_000004_add_presence_columns_to_platform_external_user_map_table` (19th on MySQL): `presence_status` ENUM(online,offline) default offline + nullable `presence_seen_at` + index `external_user_map_presence_platform (platform_id, presence_status)`; `ExternalUserMap` constants/fillable/casts.
  - **Channel naming + authorization:** `App\Chat\ChatChannels` (client-visible `private-chat.{conversation public_id}` / `presence-chat.{platform public_id}`; **bare transport names** for `PrivateChannel`/`PresenceChannel` objects — Laravel prepends `private-`/`presence-` itself, avoiding `private-private-chat.*`; parsers) + `App\Chat\ChatChannelAuthorizer` (participant / platform-member checks, `403 CHANNEL_DENIED` — never existence-disclosing 404s — Pusher-protocol signature `app_key:hmac-sha256(socket_id:channel[:channel_data], app_secret)` from server-only config; presence `channel_data` = `{user_id:"<internal id>", user_info:{external_user_id, presence_status}}`).
  - **Events:** `App\Events\RealtimeEvent` base — **`ShouldBroadcastNow`** (inline, no queue worker) + **`ShouldRescue`** (dead/unreachable transport logged + swallowed, never a 500) + `broadcastWhen()` (global `chat.realtime.enabled` kill-switch). **Removed `ShouldDispatchAfterCommit`** during testing: with `RefreshDatabase` each test runs in a transaction so after-commit callbacks never fired mid-test; call sites already broadcast strictly after their own `DB::transaction` closes (documented ordering guarantee). Concrete events `MessageCreated`, `MessageRead`, `ConversationUpdated` (reason `created`/`updated`), `UserOnline`, `UserOffline` (→ `UserPresenceChanged`) — all payloads minimal (public ULIDs + external ids), never secrets.
  - **Services:** `RealtimeBroadcaster` (single dispatch point; every method honors `enabled()`; `conversationUpdated` lazily loads participants + last-message sender); **`PresenceService`** (DB-backed, no Redis: `heartbeat` persists + bumps `seen_at`/`last_seen_at` and broadcasts ONLY on transitions; `state` lazily flips stale online→offline once; `sweep` per-row `lockForUpdate` re-check, bounded batch `presence_sweep_limit`). Wiring: `ChatMessageService::send` broadcasts on non-duplicate only; `ChatConversationService` broadcasts `conversation.updated('created')` after fresh resolve and `message.read` after `markRead`.
  - **HTTP surface (Phase 3E):** `POST /api/v1/chat/presence` (write — heartbeat, `status` optional `online|offline`, returns `changed`), `GET /api/v1/chat/presence/{external_user_id}` (read — effective/polling state, lazy stale transition, 404 for unknown/cross-platform), `POST /api/v1/chat/socket/auth` (read — server-to-server subscription auth → signed `auth` + `channel_data`); `PresenceController`, `SocketAuthController`, `PresenceHeartbeatRequest` (`in:online,offline`), `SocketAuthRequest` (`socket_id` `[0-9]+\.[0-9]+`, `channel_name` ≤120).
  - **Command:** `app/Console/Commands/ChatPresenceSweepCommand.php` — `chat:presence-sweep` (cron-every-minute; optional `--platform=<public_id|slug>`).
  - **Bugs found by tests and fixed:** `RealtimeBroadcaster::enabled()` made public (PresenceController reads it for `meta.realtime_enabled`); the `recording` broadcaster needs `broadcasting.connections.recording` config + `Broadcast::extend` + `forgetDrivers`; `PrivateChannel` double-prefix (above); closures receive raw `Channel` objects → compare via `array_map('strval', …)`; `conversation.updated` fires twice per send (`created` then `updated`); `ConversationUpdated` nullsafe-vs-nullable + `PresenceService::state()` return typing (PHPStan).
- **Tests (15 new; 208 assertions):** `tests/Support/RecordingBroadcaster.php` (process-memory recorder, `$onBroadcast` at-dispatch assertions, `$throw` fault injection) + `tests/Feature/RealtimeChatTest.php` — persist-before-any-broadcast (row exists at dispatch), minimal-secret-free payload + duplicate send never re-broadcasts, `created`+`updated` conversation events, `message.read` advance-only cursor, presence transitions only-on-change + sweep/reconnect re-announce, private-channel auth signature + deny non-participant/cross-platform, presence-channel auth + `channel_data` + foreign-platform deny, unknown channel 403 / bad socket_id 422, scope enforcement (`SERVICE_NO_ACCESS` both ways), realtime-disabled REST-quiet, **dead-transport REST resilience** (send a message + presence with the broadcaster throwing → still 201/200), polling fallback + 404s.
- **Verification (all green):** `php -l` clean on every changed file · Pint (Laravel preset) fixed 22 files → `--test` **PASS** · PHPStan level 5 **No errors** · full suite **108 tests / 908 assertions** on sqlite AND real **MySQL `myvivah`** · `migrate:fresh --force` **19**/19 migrations on MySQL (Phase 3D 93-test suite intact).
- **Docs:** `docs/realtime-chat.md` (§Real-Time Transport implemented, §Presence implemented, open decisions resolved), `docs/database.md` (presence columns), `docs/integration-api.md` (presence + socket/auth endpoints, `CHANNEL_DENIED`), `docs/security.md` (channel authorization/signing), **`docs/deployment.md` (new — shared-hosting default, optional realtime), `docs/changelog.md` (this entry), `docs/current-tasks.md`, `docs/known-issues.md`, `docs/README.md`.
- **Stop boundary honored:** chat widget UI, widget identity-token bootstrap, Reverb server install, subscriptions/payments, admin dashboard and AI agents were **NOT** started.

### Added — 2026-09-23 (Phase 3D — Chat API Foundation)

- Completed the chat REST + DB foundation per `docs/realtime-chat.md` (§REST API/§Unread Counts/§History & Pagination) in **one batch**:
  - **Schema (3 migrations `2026_09_23_000001`–`000003`, 18 total on MySQL):** `conversations` (ULID `public_id`, `platform_id` FK, deterministic `participant_key` CHAR(64) = sorted `<mapId>:<mapId>` pair, status enum, denormalized `last_message_id/at/excerpt/sender` with **no FK on last_message_id** — circular conversations↔messages, documented in the migration comment; UNIQUE `(platform_id, participant_key)`, index `(platform_id, last_message_at)`); `messages` (ULID `public_id`, `platform_id`+`conversation_id` FKs, `sender_external_user_map_id` **nullable** FK → `platform_external_user_map` SET NULL — nullable required by MySQL for SET NULL, type/status enums, `content` TEXT, `client_message_id` CHAR(64) nullable, SoftDeletes reserved for future recall; UNIQUE `(platform_id, conversation_id, client_message_id)`, cursor + created_at + sender indexes); `conversation_participants` (seat UNIQUE `(conversation_id, external_user_map_id)`, `platform_id` denormalized, read state `last_read_message_id` FK → messages SET NULL + `last_read_at` + `unread_count`, index `(platform_id, external_user_map_id)` + `(conversation_id, last_read_message_id)`).
  - **Models/factories:** `Conversation` (HasUlids→`public_id`, no SoftDeletes), `Message` (HasUlids + SoftDeletes), `ConversationParticipant`; `Platform::conversations()` relation; `ConversationFactory`/`MessageFactory`/`ConversationParticipantFactory` (tenant-aligned `forSeat`/`forConversation` helpers).
  - **Services:** `ExternalUserContext` (resolves the ACTING user from `X-External-User-Id` strictly inside the platform's map; blank/oversized → 422 `VALIDATION_FAILED`, unmapped → 404); `ChatConversationService` (deterministic resolve-or-create with transaction + unique-violation reconcile; platform-scoped participant; advance-only `markRead` — clamps to `min(requested, latest)`, never regresses, resets unread in the same transaction); `ChatMessageService` (idempotent `send` — pre-check + insert + unique-violation reconcile, touch `last_message_*` + increment ONLY other participants' unread in one transaction; cursor `history` — limit+1 probe → `has_more`, opaque internal-id cursor, ASC rows).
  - **HTTP surface under `/api/v1/chat`** (same scoped stack `ValidatePlatformToken`→`EnsurePlatformAccess`→`LogApiAudit`→`EnforcePlatformRateLimit`): `POST|GET /conversations` (create=write / list=read, last-message-first NULLS-LAST, page-based), `GET /conversations/{id}` (read; acting-user read state via `ConversationResource` custom ctor), `POST /conversations/{id}/read` (write), `GET /conversations/{id}/messages` (read, cursor), `POST /conversations/{id}/messages` (write, idempotent, `201` + `meta.duplicate`). Route param constraint `[a-zA-Z0-9]+` (Laravel ULIDs are lowercase). 5 FormRequests, 3 resources (`MessageResource`, `ConversationParticipantResource`, `ConversationResource` with acting slot), 2 controllers, `config/chat.php` tunables (participants=2, msg max 4000, client-id 8–64, excerpt 120, history 50/100, list 20/100).
  - **Bugs surfaced by tests and fixed:** route param name must match the route `{conversation}`; ULID route constraint uppercase-only would 404 (lowercase ULIDs); `distinct` rule alone did not block duplicate pairs → belt-and-braces `withValidator`/`after()` rejection + service-level `sortPair` throws 422 on <2 distinct; MySQL `errno 150` on `ON DELETE SET NULL` requires a NULLABLE FK column (sqlite is lenient).
- **Tests (12 new; 224 assertions):** `tests/Feature/ChatApiEndToEndTest.php` — deterministic resolve (order-insensitive, same thread, `meta.created`), create validation (422), idempotent send + unread only-for-others + denormalized excerpt, participant/cross-platform authz (all 404, empty list), cursor pagination (25 msgs / 3 pages / gapless / `has_more`+`next_cursor`), history + send validation (422), list ordering (NULLS-LAST, per-page, read-state), advance-only mark-read (unread reset + never-regresses), read-only-scope token (403 `SERVICE_NO_ACCESS` + `insufficient_scope` audit; reads allowed), chat requests audited, missing/unknown actor header (422/404).
- **Verification (all green):** `php -l` clean on every changed file · Pint `--test` **PASS (123 files)** · PHPStan level 5 **No errors** · PHPUnit **93 tests / 700 assertions** (sqlite) · full suite re-run against real **MySQL `myvivah`** (93/93) · `migrate:fresh` re-applied **18**/18 migrations on MySQL · `SHOW CREATE TABLE` verified UNIQUEs/FKs/indexes for `conversations`, `messages`, `conversation_participants`.
- **Docs:** `docs/database.md` (chat domain → actual schema; identity map naming; no-FK note), `docs/realtime-chat.md` (§REST API implemented, unread strategy + pagination cursor resolved), `docs/integration-api.md` (chat endpoint reference + `X-External-User-Id`), `docs/changelog.md` (this entry), `docs/current-tasks.md`, `docs/known-issues.md`, `docs/README.md`.
- **Stop boundary honored:** real-time transport (Reverb/WebSockets), Redis/queues/broadcasting, chat widget, push, subscriptions/payments, admin dashboard and AI agents were **NOT** started.

### Added — 2026-09-23 (Phase 3C — Integration API & Per-Platform Rate Limiting)

- Completed the self-service integration API + per-platform throttling per `docs/phase-3a-api-integration-plan.md` §14/§15 in **one batch**:
  - **Integration onboarding/config:** `PlatformOnboardingService::onboard()` + `integration:onboard` artisan command (idempotent; provisions platform + integration + active API key + `realtime_chat` entitlement + **system owner** `system@myvivah.local` — `platforms.created_by` is NOT NULL); `config/api.php` (`rate_limit_per_minute` default 60); `GET/PATCH /api/v1/integration/config` (scope `authentication`; `base_domain`, `allowed_origins` via `IntegrationConfigRequest`, 422 `VALIDATION_FAILED`); `IntegrationConfigResource`.
  - **Key management API:** `GET /api/v1/integration/keys` (metadata only — fingerprint/status/dates, **never** raw secrets), `POST /api/v1/integration/keys/rotate` (primary → rotated + new primary; raw secret returned exactly once in `IntegrationConfigResource`/`client_secret_visible_once`), `POST /api/v1/integration/keys/revoke` (`KeyRevokeRequest` key_fingerprint 40 chars; `ApiKeyService::revokeKey()` hard-revoke + `keyRotated` audit with `action: revoke`; isolation-scoped, unknown → `NOT_FOUND` 404).
  - **Users API refinement:** `GET /api/v1/users` paginated platform-scoped reference list (`ExternalUserReferenceResource`, `meta.pagination`, per_page cap 100); `GET /api/v1/users/{external_user_id}` now bumps `last_seen_at`; path segment constrained `[^/]+` (registered after `users/verify`).
  - **Per-platform rate limiting:** `EnforcePlatformRateLimit` middleware — keyed by the **verified** platform id (`integration:<platform_id>`), ceiling from `platform_integrations.rate_limit_per_minute` (fallback `config('api.rate_limit_per_minute')`, 60). Implemented as a plain middleware class because Laravel priority-sorts the `throttle:`/`ThrottleRequests` middleware *ahead of* custom middleware, which routed it before `ValidatePlatformToken` set the token context (investigated via middleware-order and attribute debugging; replaced the string alias). Exceeded limits throw `Illuminate\Http\Exceptions\ThrottleRequestsException` → `429 RATE_LIMITED` envelope + `Retry-After`/`retry_after_seconds`; `LogApiAudit` wraps it so 429s stay audited. `retry_after_seconds` + Retry-After header now rendered by the `bootstrap/app.php` handler.
  - **Standardized error rendering:** `bootstrap/app.php` now maps `ThrottleRequestsException` → `RATE_LIMITED` 429, other `HttpExceptionInterface` → `NOT_FOUND` 404 / `METHOD_NOT_ALLOWED` 405 / default `REQUEST_FAILED`, `HttpResponseException` pass-through, generic `Throwable` → `INTERNAL_ERROR` 500 (no details leaked). Removed the no-op `use Throwable;` (PHP 8.2 warnings from `php -l`).
  - **Audit hardening:** `LogApiAudit` rewritten with try/catch/finally + `statusFor()` so 4xx/429 responses are audited too; audit tests filter on the `api/v1/...` path prefix (`$request->path()` includes the version prefix).
  - **Platform self-description:** `GET /api/v1/platform/me` integration block now includes `base_domain`, `rate_limit_per_minute`, `token_ttl_seconds`.
- **Tests (21 new; 214 assertions):** `tests/Feature/IntegrationOnboardingTest.php` (5 — idempotency, one-time secret, namespace/slug isolation, factory equivalence) + `tests/Feature/IntegrationApiEndToEndTest.php` (16 — config CRUD + validation, keys list/rotate/revoke/grace, users list/show/last-seen, cross-platform isolation, authentication-scope enforcement, per-platform rate limit + peer isolation, 405 envelope, audit coverage). Also fixed aboard: `$platform->setRelation('integration', $integration)` in `onboard()` (Eloquent caches null relation pre-create).
- **Verification (all green):** `php -l` clean on all changed files · Pint `--test` **PASS (99 files)** · PHPStan level 5 **No errors** · PHPUnit **81 tests / 476 assertions** (sqlite) · full suite re-run against real **MySQL `myvivah`** (81/81) · `migrate:fresh` re-applied **15**/15 migrations on MySQL.
- **Docs:** `docs/integration-api.md` (new server-side v1 API reference), `docs/security.md` (rate-limiting), `docs/api-integration.md` (status), `docs/api-contract.md` (pointer), `docs/README.md` (index/status), `docs/changelog.md` (this entry), `docs/current-tasks.md`, `docs/known-issues.md`.

### Added — 2026-09-22 (Phase 3B-2 — API Integration Foundation: Key Lifecycle, Audit Trail & External User Identity)

- Completed the remaining API-integration foundation per `docs/phase-3a-api-integration-plan.md` §14 (scopes E–J) in **one batch (A–F)**:
  - **A — API key lifecycle:** `platform_api_keys.status` (`active`/`rotated`/`revoked`) + `(platform_integration_id, status)` index; **`ApiKeyService`** `rotate()` (demotes primary → rotated backup, promotes new key, returns the raw secret exactly once — never stored/logged), `revokeAll()`, `generateSecret()`; `PasetoTokenService::assertKeyUsable()` is grace-aware — a rotated key presented after `paseto.rotation_grace_seconds` (default 24 h) is **hard-revoked on sight** and rejected (`TOKEN_REVOKED`); `PlatformIntegration::primaryApiKey()` now filters on `status='active'`.
  - **B — Security audit trail:** migration `2026_09_22_000012_create_api_audit_logs_table` + append-only `ApiAuditLog` model + **`ApiAuditService`** (single fail-open writer) + **`LogApiAudit`** middleware (per-request `request` row with ip_hash + SHA-256 checksums). Events: `token_issued`, `token_rejected`, `token_expired`, `invalid_token`, `wrong_platform`, `insufficient_scope`, `key_rotation`, `request`. `ValidatePlatformToken` now audits every rejection (preserving the exception HTTP status → platform mismatch = 403), `EnsurePlatformAccess` audits each denial, `AuthController` audits issue/reject.
  - **C+D — External user identity map + v1 endpoints:** migration `2026_09_22_000011_create_platform_external_user_map_table` (UNIQUE `(platform_id, external_user_id)`, optional `local_public_id` FK → `users.public_id`), `ExternalUserMap` model/factory, **`ExternalUserService`** (strictly platform-scoped `resolve`/`resolveOrCreate` with retry-once on unique-violation), **`POST /api/v1/users/verify`** (`realtime_chat:write`; upsert, returns only `public_id`/`external_user_id`/`local_public_id`/`synced_at`/`last_seen_at` — no profile mirroring) and **`GET /api/v1/users/{external_user_id}`** (`realtime_chat:read`; 404 if unmapped; wildcard constrained to `[^/]+`; route registered after `users/verify`).
  - **E — Conversation identity reference layer:** satisfied by the external-user map (`local_public_id` reference); **no conversations/participants/messages tables created** (realtime chat remains gated).
  - **F — Tests (23 new):** `tests/Unit/ApiKeyServiceTest` (rotation/grace/hard-revoke/one-time secret), `tests/Feature/ExternalUserMappingTest` (verify idempotency, 422, cross-platform isolation, scope enforcement, DB uniqueness), `tests/Feature/SecurityAuditLogTest` (all events, platform/null-platform scoping, append-only, fail-open).
- **Verification (all green):** `php -l` clean · Pint `--test` **PASS (86 files)** · PHPStan level 5 **No errors** · PHPUnit + `artisan test` **60 tests / 262 assertions** (sqlite) · full suite re-run against real **MySQL `myvivah`** (60/60) · `migrate:fresh` re-applied all **15** migrations on MySQL with `status` column/index, map uniqueness, and audit table verified.
- **Fixes found by the sweep:** `ApiAuditService` namespace typo (was `App\Models`) + `self::DIMENSIONS` constant reference; `graceExpiresAt()` returned the wrong Carbon type; `ExternalUserService` retry-loop dead code (PHPStan level 5); test issues (service-key uniqueness, one-time-secret assertion, non-transactional MySQL DDL in the fail-open test replaced with a value-level write error so the audit table survives).
- **Docs:** `docs/security.md` (key lifecycle + audit trail), `docs/api-contract.md` (external user identity rules), `docs/api-integration.md` (backend-foundation status), `docs/changelog.md` (this entry), `docs/current-tasks.md`, `docs/known-issues.md`.

### Added — 2026-09-18 (Phase 3A — API Integration Module Plan)

- Authored `docs/phase-3a-api-integration-plan.md` — API Integration Module plan (plan only; **no code** — implementation gated per AGENTS.md phase gating): PASETO v4.local integration auth + token lifecycle, platform registration/integration config, platform-scoped authorization + entitlements (`platform_service_access`), external user ID mapping/sync, v1 endpoint contracts, versioning, validation + standardized errors, rate limiting, audit logging, cross-platform isolation, tests, required artifacts, implementation order, exit criteria, open decisions.
- Updated `docs/README.md` index.

### Added — 2026-09-22 (Phase 3B-1 — PASETO Authentication Foundation)

- Implemented the Phase 3A §17 3B-1→3B-5 auth skeleton on top of the T1–T8 schema (the first **code** from the API Integration plan):
  - **Models + factories:** `PlatformIntegration` (ULID `public_id`, 1:1 with platform, `STATUS_*` + `PASETO_V4_LOCAL` lock), `PlatformApiKey` (immutable key rows, SHA-256 fingerprint, `primary_token` CHAR(7) DB-level one-primary sentinel `PRIMARY`, `toSymmetricKey()` via Laravel `Crypt` AES-256-CBC), `PlatformServiceAccess` (service relation typed); factories `PlatformIntegrationFactory`, `PlatformApiKeyFactory`, `PlatformServiceAccessFactory` (`synced_at` now()).
  - **`PasetoTokenService`** (`app/Services/`): `issue`/`validate`/`revoke` over `paragonie/paseto` v4.local; claims `aud/platform_id/sub/kid/iat/exp/jti/scope`; TTL capped at config max (1h); scopes derived from T8 entitlements (`authentication` always present); `jti` blacklisted on revoke; validation re-checks key `revoked_at`, integration `revoked_at`+status, platform status.
  - **Middleware:** `ValidatePlatformToken` (Bearer parse → service validate → attributes `paseto`; 401 envelope), `EnsurePlatformAccess` (middleware-param scopes, dotted convention `realtime_chat:read`; T8 entitlement lookup with `authentication` bootstrap-scope exemption).
  - **Controllers/routes:** `POST /api/v1/auth/token` (client_id=platform `public_id` + client_secret=base64url API key, fingerprint-pin with `hash_equals`, returns `token_type/access_token/expires_in/scope/jti`, throttled `paseto.issue`), `POST /api/v1/auth/revoke`, `GET /api/v1/platform/me` (self-describe integration + entitlements + scopes).
  - **Requests/config:** `IssueTokenRequest` (422 → `VALIDATION_FAILED`), `config/paseto.php` (`service_scopes.realtime_chat` = read/write), `ApiException` stable-code envelope (`{success,error{code,status,message,request_id[,errors]},meta}`) rendered in `bootstrap/app.php`.
- **Migrations fixed in place (pre-production, no releases yet):** `000001` `SHOW CREATE TABLE` probed `deleted_at` → portable `Schema::hasColumn`; `000010` `primary_token` CHAR(6)→CHAR(7) to fit the `PRIMARY` sentinel; `PlatformServiceAccessFactory.synced_at` NOT NULL fix.
- **Tooling unblocked:** ran PHPStan for the first time in this project — fixed the PHPStan 2.x `cache:` legacy-string config (now structure-free, `resultCachePath`), added `parseModelCastsMethod: true` for `casts()`-method models, resolved 8 initial level-5 errors in `app/` → **`vendor/bin/phpstan` level 5 = No errors**.
- **Verification (all green):** `php -l` clean on all changed files · Pint `--test` PASS (Laravel preset, 71 files) · PHPUnit **31 tests / 102 assertions** (26 new: `tests/Unit/PasetoTokenServiceTest.php` + `tests/Feature/AuthTokenLifecycleTest.php`) via `vendor/bin/phpunit` and `php artisan test` · full feature suite re-run against real **MySQL `myvivah`** (18/73 OK) · `migrate:fresh` re-applied all 13 migrations on MySQL with `primary_token` char(7) verified.
- **Docs:** `docs/decisions.md` ADR-013 (PASETO v4.local token/lifecycle), `docs/security.md` API-token section, `docs/current-tasks.md`, `docs/phase-3a-api-integration-plan.md` status.

## [Unreleased]

### Added — 2026-09-18 (Phase 2D — Factories & Seeders, Demo Data)

- Authored **8 Eloquent factories** (`database/factories/`), 1:1 with the Phase 2B migrations + Phase 2C models — `UserFactory`, `PlatformFactory`, `PlatformAdminFactory`, `ServiceFactory`, `PlanFactory`, `SubscriptionFactory`, `PaymentFactory`, `PlatformServiceAccessFactory`.
  - ULID `public_id` on the 4 external-facing models (User/Platform/Plan/Subscription via `HasUlids` auto-generating for `UserFactory::create()`); T3/T4/T7/T8 remain internal-ULID-free per `docs/phase-2a-schema-plan.md` §6.
  - Cast fidelity: `decimal:2` INR columns, billing-period enums, bool flags (`is_active`, `is_default`, `has_default_currency`), nullable `public_id` gaps on Subscription ULID external mapping — matches casts in Phase 2C models.
  - `PaymentFactory` gateway fields routed through the **manual/offline** gateway per ADR-011 (Bay area; no online payment gateway at MVP) — `gateway` nullable, `gateway_transaction_id` unique nullable, `status` enum(pending,completed,failed,refunded).
- Authored **5 seeders** (`database/seeders/`): `DatabaseSeeder` (orchestrator), `DemoUserSeeder`, `ServiceSeeder`, `PlanSeeder`, `DemoPlatformSeeder`.
  - **Idempotent by construction:** every child uses `updateOrCreate` on the natural unique key (`email`, service `key`, `[service_id, plan_key]`, platform `slug`, `[platform_id, user_id]`), so re-running `db:seed` / `migrate:fresh --seed` never duplicates (proved by second consecutive successful run with identical row counts).
  - FK ordering enforced by `DatabaseSeeder::$call([...])` (DemoUser → Service → Plan → DemoPlatform).
  - **Demo policy honored:** fake `example.test` emails/domains, fake Indian-style `+91` phone numbers, INR demo pricing, demo password hash only — **no real user data, no real platform data, no payment/phone/credential values that could belong to a real person** (docs/AGENTS.md demo-only rules).
- **Verification (Phase 2D exit) — all real, all green:**
  - `php -l` clean on all 21 files (8 factories + 8 models + 5 seeders).
  - Pint (Laravel preset) clean — 21/21 files, 0 issues after style fix.
  - PHPUnit suite `2/2` passing.
  - `php artisan db:seed` **twice** — both DONE; row counts stable (users 3, services 1, plans 3, platforms 1, platform_admins 1); duplicate/unique/FK-orphan probes all `0`.
  - `php artisan migrate:fresh --seed` — all 11 migrations re-applied cleanly + full re-seed successful (repeatability proven).
- **Composer limitation (honestly reported):** `composer dump-autoload` and PHPStan cannot run in this environment — Composer.phar is absent and vendor autoloading was validated by direct PSR-4 `class_exists` probes instead; static analysis is documented as not-runnable rather than claiming a pass.

### Added — 2026-09-18 (Phase 2C — Core Platform Eloquent Models)

- Authored 8 Eloquent models in `app/Models/` — 1:1 with the Phase 2B migrations (T1–T8) and `docs/database.md`:
  - `User` (+ → `platforms`, `platformAdmins`) — scaffold upgraded per 2A plan: `HasUlids`/`uniqueIds` = `public_id`, `SoftDeletes`, timezone/locale/last_login providers, ULID `public_id` fillable.
  - `Platform` — `HasUlids` + `HasFactory` + `SoftDeletes` (SoftDeletes: `public_id` deleted_at); `creator` (created_by), `admins` belongsToMany via `platform_admins` (withPivot role/invited_at/accepted_at), `platformAdmins`, `subscriptions`, `payments`, `serviceAccess`; STATUS_* constants.
  - `PlatformAdmin` — platform/user belongsTo; casts invited_at/accepted_at datetime; no ULID, no soft delete.
  - `Service` — SoftDeletes; `plans`, `subscriptions`, `platformServiceAccess`; casts is_active bool, sort_order int; internal-only, no ULID (to public contract per §6).
  - `Plan` — HasUlids + SoftDeletes; decimal:2 price, array features, bool is_active, int billing_interval/sort_order; `service`, `subscriptions`.
  - `Subscription` — HasUlids (public_id); casts datetimes; `platform`, `service`, `plan`, `payments`; no soft delete.
  - `Payment` — `subscription`, `platform`; casts decimal:2 amount (ASC gateway txn unique), metadata array, paid_at datetime; no ULID/soft delete (financial retention).
  - `PlatformServiceAccess` — `platform`, `service`; has_access bool, effective_until/synced_at datetime; no ULID/soft delete; table `platform_service_access`.
- **Verification for Phase 2C (all green):**
  - `php -l` clean on all 8 models.
  - Pint (Laravel preset) clean — `0` style issues across `app/Models`.
  - Test suite `2/2` passing.
- **Gated (not created — AGENTS.md phase gating):** Eloquent factories and seeders remain deliberately NOT created; Phase 2D pending approval.
- **ADR-002 (ULID `public_id`):** applied to the 4 external-facing models (User, Platform, Plan, Subscription); Service/Payment/PlatformAdmin/PlatformServiceAccess remain internal-ULID-free per plan §6.

### Added — 2026-09-18 (Phase 2B — Core Platform Migrations)

- Authored and ran the **Phase 2B business migrations**, 1:1 with `docs/phase-2a-schema-plan.md` (T1–T8):
  - `2026_09_18_000001_add_core_platform_columns_to_users_table` — adds to `users`: `public_id` (CHAR(26) ULID, unique), `phone` (unique, nullable), `status` ENUM(active,suspended,deactivated) default `active`, `timezone`, `locale`, `last_login_at`, soft deletes (`deleted_at`). Scaffold `users` table left UNTOUCHED — this migration **alters** it (per plan: do not edit `0001_01_01_000000_create_users_table` in place).
  - `2026_09_18_000002_create_platforms_table` — InnoDB, ULID `public_id`, unique `slug`, `status` ENUM(pending,active,suspended,deactivated) default `pending`, `created_by` FK→users, soft delete.
  - `2026_09_18_000003_create_platform_admins_table` — `platform_id` FK, `user_id` FK, `role` ENUM(owner,admin,developer) default `admin`, `invited_at`/`accepted_at`, UNIQUE(platform_id,user_id); no soft delete.
  - `2026_09_18_000004_create_services_table` — unique `key`, `is_active` bool default false, soft delete.
  - `2026_09_18_000005_create_plans_table` — `service_id` FK, ULID `public_id`, unique `plan_key`, price/currency(INR)/billing_period, `is_active` bool default false, soft delete.
  - `2026_09_18_000006_create_subscriptions_table` — platform/service/plan FKs, `status` ENUM(pending,active,expired,cancelled,suspended) default `pending`, `auto_renew` bool default false; no soft delete.
  - `2026_09_18_000007_create_payments_table` — subscription/platform FKs, `status` ENUM(pending,completed,failed,refunded) default `pending`, unique `gateway_transaction_id`; no soft delete.
  - `2026_09_18_000008_create_platform_service_access_table` — platform/service FKs, `has_access` bool default false, UNIQUE(platform_id,service_id), no soft delete.
- All 8 business migrations **verified applied** on `myvivah` (e2e: `migrate:status` + schema probe — all tables + `users` additions + `platform_service_access` unique index confirmed).
- **M8 fixed in-place during the run:** `synced_at` → `DEFAULT CURRENT_TIMESTAMP` to satisfy MySQL 8.0.22 strict `NO_ZERO_DATE` (global doesn't allow implicit zero timestamp for a bare `TIMESTAMP NOT NULL`).
- **Still gated (no work done):** Eloquent models for T1–T8, factories, and seeders — require separate approval per phase-gating (AGENTS.md / `docs/current-tasks.md` Phase 2C).

### Added — 2026-09-18 (Phase 2A — Core Platform Schema Plan)

- Authored `docs/phase-2a-schema-plan.md` — authoritative Core Platform Domain schema contract:
  - Scope: `users`, `platforms`, `platform_admins`, `services`, `plans`, `subscriptions`, `payments`, `platform_service_access` (T1–T8).
  - Documented as **plan only** — no migrations, models, factories, or seeders created; Phase 2B migration work gated on approval (AGENTS.md phase-gating).
  - Staged out of scope: integration (Phase 3), widget (Phase 4), real-time chat (Phase 5), and future AI domains (AGENTS.md §17/§23) — boundary statement only, no premature tables.
  - Set binding conventions (1:1 with `docs/database.md` / `docs/laravel-architecture.md` / `docs/decisions.md` / `docs/platform-isolation.md` / `docs/security.md`): BIGINT UNSIGNED internal PKs + `CHAR(26)` ULID `public_id`; InnoDB; `utf8mb4` / `utf8mb4_unicode_ci`; booleans `is_`/`has_`/`can_` with `DEFAULT false`; soft-delete matrix; explicit indexes; `platform_id` isolation on every platform-owned table.
  - Fresh Bootstraps-ready open items documented (cache-vs-compute entitlement, single-owner policy, `payment` gateway ADR-011, plan JSON contract in Phase 2B).
  - Added the **mysql_native_password** note (legacy-plugin escape hatch only; not switched by default) — new, not previously documented.
- Updated `docs/README.md` (index + project status) to reference the new plan.

### Added — 2026-09-17 (Phase 1 — Laravel Bootstrap)

- Initialized **Laravel 12.69.2** (framework skeleton, no business code): PHP 8.2.12 (Windows XAMPP), Composer 2.9.5.
- Configured MySQL connection (`myvivah` database, 127.0.0.1) + framework default migrations; **no business migrations created**.
- Configured queue/cache/session on the database driver (no Redis server provisioned in this environment yet); `REDIS_*`, `QUEUE_*`, `BROADCAST_*`, and Reverb keys are present and documented (ADR-010 remote target) but **not activated**.
- Installed dev-only tooling: Pint (skeleton) + Larastan/PHPStan (dev-only); added `pint.json` (Laravel preset) and `phpstan.neon` (level 5).
- Created `docs/bootstrap.md` — environment report, versions, files, commands, validation results, open decisions.

---

### Added — 2026-09 (Architecture Review)

- Added `docs/architecture-review.md` — pre-bootstrap review of the full documentation set:
  - Confirmed 16 consistent decisions
  - Identified 13 contradictions/inconsistencies, 11 missing requirements
  - Reviewed database design (6 issue groups), API/auth (7), subscription/payment (6), real-time chat (8), security/isolation (7)
  - Provided 15 prioritized corrections and a 21-item approval list
  - Proposed final architecture baseline and adjusted implementation order
  - Verdict: ready for Laravel bootstrap after corrections 10.1–10.3/10.6–10.7 and section-11 approvals
- `docs/README.md` index updated to include the review document.

### Added — 2026-09 (Documentation Foundation)

- Created the documentation foundation for MyVivahAI:
  - `docs/README.md` — documentation index and reading order
  - `docs/product-overview.md` — product vision, services, target users
  - `docs/architecture.md` — system architecture and module boundaries
  - `docs/registration-flow.md` — account registration, verification, login, onboarding
  - `docs/subscription-flow.md` — plans, payments, subscription lifecycle, entitlements
  - `docs/api-integration.md` — client API integration dashboard flow
  - `docs/api-contract.md` — external API capabilities and contracts
  - `docs/widget-integration.md` — widget configuration, installation, testing
  - `docs/realtime-chat.md` — chat architecture, delivery, presence, authorization
  - `docs/database.md` — conceptual database design for all domains
  - `docs/security.md` — security requirements and threat considerations
  - `docs/platform-isolation.md` — multi-platform isolation strategy
  - `docs/laravel-architecture.md` — recommended Laravel structure and conventions
  - `docs/future-services.md` — extension model for future AI services
  - `docs/current-tasks.md` — task list (no tasks completed beyond docs)
  - `docs/changelog.md` — this changelog
  - `docs/known-issues.md` — open uncertainties and decisions to confirm
  - `docs/decisions.md` — Architecture Decision Records

**Project status:** Documentation only. No application code has been written, no migrations exist, and the Laravel project has not yet been initialized in the workspace.
